# Core dependencies
mcp>=1.0.0,<2.0.0
asyncpg>=0.29.0
uvicorn>=0.29.0
python-dotenv>=1.0.0
starlette>=0.35.0

# Testing dependencies
pytest>=7.0.0
pytest-asyncio>=0.21.0
pytest-cov>=4.0.0

# Optional performance dependencies (Unix/macOS only)
uvloop>=0.17.0 ; sys_platform != "win32"
httptools>=0.5.0 ; sys_platform != "win32"
